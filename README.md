### tiny-parser

```
# show all tags
$ git tag -l

# commit new code

$ git checkout -b [new branch name] [old tag name]
# ... modify code
$ git add .
$ git commit -m "..."
$ git tag [new tag name]
$ git push --tags
```